marimo/_lsp/
__marimo__/

# MLflow that stores experiment runs in the default directory
mlruns/
temp_aug/
temp_heatmaps/
temp_overlays/
temp_uncert/

# Personal scripts for batch inference
batch_run.py
report_generator.py
robust_metrics.py
test_mlflow_connection.py

# Checkpoints (large binary files — commit only intentionally)
checkpoints/*.pth

# Results — keep structure via .gitkeep, ignore bulk generated images
# Commit only curated final figures used in the report
results/figures/qualitative/*.png
results/figures/quantitative/*.png
results/figures/training/*.png
# Tables are small CSVs — commit them
!results/tables/*.csv

# Data - commit only empty folder, ignore all raw data and intermediate files
data/Bengin cases
data/Malignant cases
data/Normal cases
data/*.txt

# Private report folder — not committed
report/
/src/__pycache__
